<!-- ============================================
     EXPENSES COMPONENT - PLACEHOLDER
     ============================================
     
     Temporary placeholder for expenses tracker.
     Shows expense list without full functionality.
============================================ -->

<div class="expenses-placeholder">
    <div class="placeholder-message">
        <h3>💰 Expenses Tracker</h3>
        <p>Expenses tracker component is being implemented.</p>
        <p>Expenses will appear here soon!</p>
        
        <% if (expenses && expenses.length > 0) { %>
            <div class="expense-count">
                <strong><%= expenses.length %></strong> expense<%= expenses.length !== 1 ? 's' : '' %> in this group
            </div>
        <% } else { %>
            <div class="empty-state">
                <p>No expenses yet. Add your first expense!</p>
            </div>
        <% } %>
    </div>
</div>

